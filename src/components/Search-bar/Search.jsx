import './Search.css'

export const Search=({placeholder,onChange})=>{
    return(
        <div>
{
      <input type="search" placeholder={placeholder} onChange={onChange}/>
}

        </div>
    )

}